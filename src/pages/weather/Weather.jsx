import PageWrapper from '../PageWrapper';
import styles from './Weather.module.css';

const Weather = () => {
    return (
        <PageWrapper>
            <h1>Weather page</h1>
        </PageWrapper>
    );
}

export default Weather;
